import { AccommodationsFeatures } from './accommodations-features.model';

describe('AccommodationsFeatures', () => {
  it('should create an instance', () => {
    expect(new AccommodationsFeatures()).toBeTruthy();
  });
});
